# pubspec.yaml
name: get_gains
description: A personal workout tracking application
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  # Database
  sqflite: ^2.3.0
  path: ^1.8.3
  # State Management
  provider: ^6.1.1
  # Utilities
  intl: ^0.18.1
  uuid: ^4.2.1
  audioplayers: ^5.2.1
  flutter_local_notifications: ^18.0.1
  numberpicker: ^2.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1

flutter:
  uses-material-design: true